# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-lens
services:
  api:
    project: ./backend
    language: js
    host: containerapp
  expo-proxy:
    project: ./expo-cloud-proxy
    language: js
    host: containerapp
